<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Connexion - EduShare</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <style>
        body {
            background-image: url('https://external-content.duckduckgo.com/iu/?u=https%3A%2F%2Fc.wallhere.com%2Fphotos%2F41%2Fd1%2Fclouds_sky_mountains_snowy_mountain_horizon_Mount_Logan_Yukon_Canada-1759983.jpg!d&f=1&nofb=1&ipt=6321354ab3cf8bacd32eb000abc385cc494b7b9fbcd758e626b7f6ac8f053bf0&ipo=images'); /* Remplacez par l'URL de votre image de nuages */
            background-size: cover; /* Remplit tout l'arrière-plan */
            background-position: center; /* Centre l'image */
            height: 100vh; /* Prend la hauteur de la vue */
            display: flex; /* Utilisation de flexbox pour centrer le contenu */
            justify-content: center; /* Centre horizontalement */
            align-items: center; /* Centre verticalement */
            font-family: 'Arial', sans-serif; /* Police simple et moderne */
        }
        .card {
            background-color: rgba(0, 0, 0, 0.8); /* Noir transparent */
            border: none;
            border-radius: 20px; /* Coins très arrondis */
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.5); /* Ombre plus marquée */
            color: white; /* Texte en blanc pour contraste */
            padding: 20px; /* Espacement intérieur */
            width: 100%; /* 100% de largeur */
            max-width: 400px; /* Largeur maximale de 400px */
            text-align: center; /* Centre le contenu textuel */
        }
        .logo {
            font-size: 40px; /* Taille de l'icône */
            margin-bottom: 10px; /* Espace sous l'icône */
        }
        .project-title {
            font-size: 24px; /* Taille du titre */
            margin-bottom: 20px; /* Espace sous le titre */
        }
        .btn-custom {
            background-color: #007acc; /* Couleur personnalisée pour le bouton */
            border: none;
            border-radius: 5px; /* Coins arrondis pour le bouton */
            transition: background-color 0.3s, transform 0.2s; /* Animation de transition */
        }
        .btn-custom:hover {
            background-color: #005ea6; /* Couleur plus foncée au survol */
            transform: scale(1.05); /* Zoom au survol */
        }
        .form-group label {
            color: #ffffff; /* Couleur des étiquettes */
        }
        .alert {
            margin-bottom: 20px; /* Espace supplémentaire pour les alertes */
        }
        .footer-link {
            color: #007acc; /* Lien coloré */
            text-decoration: none; /* Pas de soulignement */
        }
        .footer-link:hover {
            text-decoration: underline; /* Soulignement au survol */
        }
    </style>
</head>
<body>
    <div class="card">
        <div class="logo">
            <i class="fas fa-cloud-upload-alt"></i> <!-- Icône du logo -->
        </div>
        <h1 class="project-title">EduShare</h1> <!-- Titre du projet -->
        <% if (messages.error) { %>
            <div class="alert alert-danger"><%= messages.error %></div>
        <% } %>
        <form action="/login" method="POST">
            <div class="form-group">
                <label for="username">Nom d'utilisateur</label>
                <input type="text" class="form-control" id="username" name="username" required>
            </div>
            <div class="form-group">
                <label for="password">Mot de passe</label>
                <input type="password" class="form-control" id="password" name="password" required>
            </div>
            <button type="submit" class="btn btn-custom btn-block">Se connecter</button> <!-- Bouton personnalisé -->
        </form>
        <div class="text-center mt-3">
            <a href="#" class="footer-link">Mot de passe oublié ?</a> <!-- Lien coloré -->
        </div>
    </div>

    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.0.7/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
