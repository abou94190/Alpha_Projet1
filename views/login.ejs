<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Connexion - EduShare</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <link rel="stylesheet" href="../public/css/style.css"> <!-- Assurez-vous que le chemin est correct -->
    <style>
        body {
            background-color: #e0f7fa; /* Fond bleu clair */
        }
        .project-title {
            text-align: center;
            margin-bottom: 20px;
            color: #00796b; /* Couleur du texte */
            font-weight: bold;
            font-size: 28px; /* Taille du titre */
            text-shadow: 1px 1px 2px rgba(0, 0, 0, 0.2); /* Ombre portée pour le texte */
        }
        .btn-custom {
            background-color: #00796b; /* Couleur personnalisée pour le bouton */
            border: none;
            transition: background-color 0.3s, transform 0.2s; /* Animation de transition */
        }
        .btn-custom:hover {
            background-color: #004d40; /* Couleur plus foncée au survol */
            transform: scale(1.05); /* Zoom au survol */
        }
        .alert {
            margin-bottom: 20px; /* Espace supplémentaire pour les alertes */
        }
    </style>
</head>
<body>
    <div class="container d-flex align-items-center justify-content-center" style="height: 100vh;">
        <div class="card" style="width: 25rem;">
            <div class="card-body">
                <h1 class="project-title">EduShare</h1> <!-- Titre du projet -->
                <h5 class="card-title text-center">Connexion</h5>
                <% if (messages.error) { %>
                    <div class="alert alert-danger"><%= messages.error %></div>
                <% } %>
                <form action="/login" method="POST">
                    <div class="form-group">
                        <label for="username">Nom d'utilisateur</label>
                        <input type="text" class="form-control" id="username" name="username" required>
                    </div>
                    <div class="form-group">
                        <label for="password">Mot de passe</label>
                        <input type="password" class="form-control" id="password" name="password" required>
                    </div>
                    <button type="submit" class="btn btn-custom btn-block">Se connecter</button> <!-- Bouton personnalisé -->
                </form>
                <div class="text-center mt-3">
                    <a href="#" style="color: #00796b;">Mot de passe oublié ?</a> <!-- Lien coloré -->
                </div>
            </div>
        </div>
    </div>

    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.0.7/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
