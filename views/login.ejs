<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Connexion - EduShare</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <link rel="stylesheet" href="/css/style.css"> <!-- Assurez-vous que le chemin est correct -->
    <style>
        body {
            background-color: #d9eaf7; /* Bleu clair pour le fond */
        }
        .card {
            border-radius: 10px; /* Arrondir les coins de la carte */
        }
        .btn-custom {
            background-color: #3498db; /* Couleur bleu personnalisée */
            color: white;
            border-radius: 5px; /* Arrondir les coins des boutons */
        }
        .btn-custom:hover {
            background-color: #2980b9; /* Couleur au survol pour le bouton */
        }
        .title {
            text-align: center; /* Centrer le titre */
            margin-bottom: 20px; /* Espacement sous le titre */
        }
    </style>
</head>
<body>
    <div class="container d-flex align-items-center justify-content-center" style="height: 100vh;">
        <div class="card" style="width: 25rem;">
            <div class="card-body">
                <h1 class="title">EduShare</h1> <!-- Ajout du titre EduShare -->
                <h5 class="card-title text-center">Connexion</h5>
                <% if (messages.error) { %>
                    <div class="alert alert-danger"><%= messages.error %></div>
                <% } %>
                <form action="/login" method="POST">
                    <div class="form-group">
                        <label for="username">Nom d'utilisateur</label>
                        <input type="text" class="form-control" id="username" name="username" required>
                    </div>
                    <div class="form-group">
                        <label for="password">Mot de passe</label>
                        <input type="password" class="form-control" id="password" name="password" required>
                    </div>
                    <button type="submit" class="btn btn-custom btn-block">Se connecter</button> <!-- Bouton stylisé -->
                </form>
                <div class="text-center mt-3">
                    <a href="#">Mot de passe oublié ?</a>
                </div>
            </div>
        </div>
    </div>

    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.0.7/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
